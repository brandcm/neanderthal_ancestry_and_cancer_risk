This directory contains a pipeline and associated script to retrieve and LD-clump significant GWAS variants.

- `significant_GWAS_variants_pipeline.sh` is a Snakemake pipeline that retrieves and LD-clumps significant GWAS variants.

- `retrieve_significant_GWAS_variants.py` is implemented in the above pipeline to parse GWAS summary stats and retrieve significant variants.